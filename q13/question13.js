"use strict";
let modeOfTransport = ["bike", "car", "bus", "plan", "train"];
// modeOfTransport.forEach(elem =>console.log(`i would like to own a ${elem}`))
for (let index = 0; index < modeOfTransport.length; index++) {
    const element = modeOfTransport[index];
    console.log(`i would like to own a ${element}`);
}
