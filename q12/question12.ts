let greeting= "Good morning";
let namess=['anma','ayesha','alia','urwa'];
// names.forEach(Element => console.log( greeting +`\t` + Element))
for (let index = 0; index < namess.length; index++) {
    const element = namess[index];
    console.log( greeting +`\t` + element)
 
    
}